# MDM

The functional documentation of the master-data engine lives next to the code: **`l1/mdm/README.md`**
(record model, visibility, roles, relationships, tags and login, prospects, lookups and their cost,
invariants, file map). Shapes are defined in `l1/mdm/defs/ontology.ts`; the facade modules call is
`l1/mdm/layer_3_usecases/mdmFacade.ts`. Agent-facing summary: `mls-102035/l2/agentNewSolution5/skills/mdm.md`.
